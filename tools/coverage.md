# SIFT manifest coverage

Generated by `tools/sift_manifest.py` from teamdfir/sift-saltstack (server image).

- **366** distinct forensic commands resolved from the SIFT server install.
- **35** of them have forensic-knowledge (FK) enrichment available; **31** are already wired into the run_command catalog (**88%** of FK-backed tools), **4** are not yet (bucket B — the actionable enrichment wins).
- The remaining **331** are the long tail: runnable via run_command today, just unenriched.

## Buckets

| Bucket | Meaning | Count |
|---|---|---|
| A | in catalog **and** FK-enriched | 31 |
| B | FK knowledge exists, **no catalog entry** (enrichment win) | 4 |
| C | neither — long tail, runnable via run_command | 331 |
|   | &nbsp;&nbsp;of which already in catalog but unenriched | 2 |

## B — enrichment targets (FK exists, add a catalog entry)

| command | package | source | FK tool |
|---|---|---|---|
| `WxTCmd` | zimmerman | manual | WxTCmd |
| `densityscout` | densityscout | manual | densityscout |
| `hindsight.py` | hindsight | pip | hindsight |
| `photorec` | testdisk | apt | photorec |

## A — covered (catalog + FK)

`7z`, `AmcacheParser`, `AppCompatCacheParser`, `EvtxECmd`, `JLECmd`, `LECmd`, `MFTECmd`, `RBCmd`, `RECmd`, `SBECmd`, `SQLECmd`, `blkls`, `bstrings`, `bulk_extractor`, `dc3dd`, `ewfacquire`, `ewfmount`, `exiftool`, `fls`, `hashdeep`, `icat`, `log2timeline.py`, `mactime`, `mmls`, `psort.py`, `rip.pl`, `ssdeep`, `tshark`, `vol`, `vshadowinfo`, `vshadowmount`

## C — long tail (unenriched, runnable)

Cataloged but no FK enrichment: `file`, `jq`

Not in catalog, no FK (sample of the long tail):
`7za`, `INDXParse.py`, `MFTINDX.py`, `MFTView.py`, `RecentFileCacheParser`, `SDS_get_index.py`, `aeskeyfind`, `affcat`, `affcompare`, `affconvert`, `affcopy`, `affcrypto`, `affdiskprint`, `affinfo`, `affix`, `affrecover`, `affsegment`, `affsign`, `affstats`, `affuse`, `affverify`, `affxml`, `ag`, `aircrack-ng`, `amcache.py`, `analyzemft`, `arp-fingerprint`, `arp-scan`, `autopsy`, `avfs`, `bdeinfo`, `bdemount`, `blkcalc`, `blkcat`, `blkstat`, `bodyfile.pl`, `cabextract`, `capinfos`, `ccrypt`, `clamav`, `cmospwd`, `cryptcat`, `dcfldd`, `ddrescue`, `ddrescuelog`, `debugvmfs`, `disktype`, `dislocker`, `dislocker-bek`, `dislocker-file`, `dislocker-find`, `dislocker-fuse`, `dislocker-metadata`, `doc_loadbios`, `docfdisk`, `driftnet`, `dsniff`, `dump-mft-entry.pl`, `dumpcap`, `editcap`, `ent`, `epic5`, `esedbexport`, `esedbinfo`, `etherape`, `ettercap-graphical`, `evtexport`, `evtinfo`, `evtparse.pl`, `evtrpt.pl`, `evtx_dump`, `evtx_dump_chunk_slack`, `evtx_dump_json`, `evtx_eid_record_numbers`, `evtx_extract_record`, `evtx_filter_records`, `evtx_info`, `evtx_record_structure`, `evtx_structure`, `evtx_templates`

## needs_review (inferred command names — not dpkg-confirmed)

`7z`, `7za`, `aircrack-ng`, `autopsy`, `avfs`, `capinfos`, `ccrypt`, `clamav`, `cmospwd`, `cryptcat`, `driftnet`, `dsniff`, `dumpcap`, `editcap`, `epic5`, `etherape`, `ettercap-graphical`, `exfatprogs`, `gawk`, `gdb`, `gzrt`, `hydra`, `hydra-gtk`, `lft`, `mergecap`, `netpbm`, `netsed`, `netwox`, `nikto`, `ophcrack`, `pdftk-java`, `pev`, `pv`, `rar`, `rsakeyfind`, `socat`, `ssldump`, `sslsniff`, `stunnel4`, `tcpreplay`, `tcpslice`, `tcptrack`, `tshark`

## Notes

- The SIFT *server* image installs the full tool set; the desktop image adds only config, so `desktop_only` is `false` throughout.
- Command names are ground-truthed from `dpkg -L` on a live SIFT box where available; `inferred: true` marks names resolved from the curated map or package name and listed under needs_review.
- Denied binaries are never emitted: `env`, `halt`, `init`, `kill`, `killall`, `mkfs`, `mkfs.btrfs`, `mkfs.ext4`, `mkfs.ntfs`, `mkfs.xfs`, `nc`, `ncat`, `pkill`, `poweroff`, `printenv`, `reboot`, `shutdown`.
